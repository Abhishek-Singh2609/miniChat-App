<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your chat</title>
</head>
<body>
    <p>Edit Your Chat : <%= chats.id %></p>
    <p>This chat was sent from <b><%= chats.from %></b> to <b><%= chats.to %></b> </p>
<form method="post" action="/chats/<%= chats.id %>?_method=PUT">
    <textarea name="msg" cols="50" rows="6"><%= chats.msg %></textarea><br>
    <input type="hidden" name="updated_at" value="<%= chats.updated_at %>">
    <button id="deleteBtn">Edit</button>
</form>
</body> 
</html>